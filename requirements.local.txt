# 本地完整開發環境（爬蟲、AI、影片合成、上傳等）
# 安裝：pip install -r requirements.local.txt

# ── Web UI + DB（Railway 也用這份）──
-r requirements.txt

# ── Module 1：爬蟲 ──
feedparser>=6.0.11
httpx>=0.27.0
aiohttp>=3.9.5
beautifulsoup4>=4.12.3
lxml>=5.2.2
playwright>=1.45.0
python-dateutil>=2.9.0
tldextract>=5.1.2

# ── LLM ──
anthropic>=0.34.0

# ── Module 2：Telegram（可選）──
python-telegram-bot>=21.3

# ── Module 6：影片 / 字幕 ──
Pillow>=10.4.0
ffmpeg-python>=0.2.0
# openai-whisper>=20240930   # 需要 CUDA，單獨安裝

# ── Module 7：YouTube 上傳 ──
google-api-python-client>=2.140.0
google-auth-oauthlib>=1.2.1

# ── Module 9：排程 ──
APScheduler>=3.10.4

# ── Module 4：TTS（之後補，需要 CUDA）──
# pip install TTS            # Coqui XTTS-v2（獨立安裝避免衝突）
# pydub>=0.25.1
# noisereduce>=3.0.2
# elevenlabs>=1.5.0

# ── Module 5：ComfyUI（透過 API 呼叫，不需安裝）──
# websocket-client>=1.8.0
